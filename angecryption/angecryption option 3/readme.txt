
a angecryption program, that decrypt / encrypt pdf/png file inside other pdf/png  file.

this verion is the reversed angecryption:
(in the "hide-file1-file2" its encrypt file1, then concat file2 to it, and then decrypt it all - [file1 returns to be a readable file...] ).
(in the "decrypt-to-file" its encrypt the full file, and then make a swap between the files...).


requires:
need to download: pycryptodome library.